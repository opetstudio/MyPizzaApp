import { StyleSheet, Dimensions, Platform } from 'react-native';

export default StyleSheet.create({
    allContainer: {
        flex: 2,
        marginLeft: 15,
        marginRight: 15,
        //backgroundColor: 'red'
      }
});
